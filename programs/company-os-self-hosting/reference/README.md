# Observation Gateway Reference

This directory preserves the independently reviewed project-local Gate 2
mechanics that predate the canonical repository. It performs no provider calls
and writes no governed state. It is not imported by the controller and is not
runtime evidence.

The next runtime slice must port the contract into canonical controller state,
retain its adversarial tests, and obtain fresh independent acceptance before
any provider launch is enabled.
