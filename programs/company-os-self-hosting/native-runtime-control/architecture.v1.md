# Native runtime control architecture v1

SQLite remains the project-bound authority. Admission and one durable dispatch intent commit before interactive host task creation. Returned native task identity is bound afterward as observed state. Requested and observed model identity remain separate. Cancellation request, host stop intent, acknowledgement, terminal observation, receipt, and reconciliation are distinct monotonic states. Repository code never calls Codex app tools.
