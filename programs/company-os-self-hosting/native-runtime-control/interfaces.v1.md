# Native runtime control interfaces v1

The controller emits an admitted dispatch record for the interactive master. The master invokes native create/read/wait/message tools and records only returned host observations. Stable manager and worker behavior comes from $manage-company-program and $execute-bounded-task. All retries use exact idempotency keys; cancellation and reconciliation require separately bound authority.
