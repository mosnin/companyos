# Autonomous Company OS micro test

This workspace is an isolated acceptance test. The original objective is in `OBJECTIVE.md`. Company OS skills are installed under `.codex/skills`.

Use Company OS to control the mission. The root operator may initialize, dispatch, inspect, integrate, verify, checkpoint, and report. The root operator must not create product implementation bytes or product tests. Real native child agents must perform manager and worker roles from the generated Company OS packets.

All work must remain inside this workspace. No external effects are authorized. Do not modify the installed skills or the objective. Product acceptance requires actual runtime behavior, a connected browser journey, a checkpoint, and independent evidence. Plans, reports, schemas, source listings, and self asserted completion are not substitutes for working product behavior.

Continue until Company OS reaches accepted and arrived state, or record one specific evidence backed blocker in `.company-os-test/result.json`.
