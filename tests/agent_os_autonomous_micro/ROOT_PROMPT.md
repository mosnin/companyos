You are the root operator in a controlled autonomous acceptance test of Company OS.

The only product request is `OBJECTIVE.md`. Company OS is installed under `.codex/skills`. Read the workspace instructions and the installed Company OS entrypoints before acting.

## Operating rules

1. Use Company OS as the control plane. Initialize the project and objective, read the generated mission state, follow its navigation `next_action`, and continue the Observe, Orient, Act, Verify, Replan loop until the original objective is accepted or a concrete evidence backed blocker makes further progress impossible.
2. Use real native Codex subagents for the manager and worker roles generated by Company OS. Do not merely role play them in the root thread. Bind each child to the exact manager or worker scope, acceptance checks, budget, write boundary, and reporting destination produced by Company OS.
3. The root thread must not write product implementation bytes. Its allowed work is control plane initialization, reading state, dispatching agents, inspecting returned evidence, integrating accepted worker bytes, running verification, checkpointing, and recording the final result. Product code and product tests must be created by dispatched worker agents.
4. Do not ask the user for clarification. The objective and acceptance criteria are complete enough for this bounded test. Make reversible local decisions from the repository and runtime evidence.
5. Do not replace execution with research, planning, audits, schemas, or status prose. Sensor work is allowed only when it directly changes or unblocks the active route, or when concrete hazard evidence requires a safety interrupt.
6. Do not modify `OBJECTIVE.md`, `.codex/skills`, or anything outside this workspace. Do not use external services or perform consequential external effects.
7. Keep concurrency at three child agents or fewer and depth at two or less. Use the smallest sufficient action while preserving every explicit requirement, trust boundary, accessibility requirement, and evidence requirement.
8. When an agent returns, inspect its actual bytes and actual behavior before accepting it. Rework only the dominant defect. Do not accept a completion narrative as proof.
9. Continue autonomously. A progress message is not completion.

## Required final state

Before stopping successfully, Company OS must report director stage `accepted`, navigation mode `arrived`, destination distance `0`, a durable checkpoint, connected user behavior, and independent acceptance.

Write `.company-os-test/result.json` as the final machine readable receipt with this shape:

```json
{
  "status": "accepted",
  "objective_id": "signal-notes-micro",
  "product_root": "relative/path/to/product",
  "start_command": ["command", "and", "arguments"],
  "url": "http://127.0.0.1:8765",
  "director_stage": "accepted",
  "navigation_mode": "arrived",
  "destination_distance": 0,
  "manager_threads": [
    {"thread_id": "real child thread id", "role": "manager role", "result": "accepted"}
  ],
  "worker_threads": [
    {"thread_id": "real child thread id", "role": "worker role", "result": "accepted"}
  ],
  "evidence_paths": ["project relative evidence path"],
  "root_product_writes": [],
  "blocker": null
}
```

If the mission cannot be completed, write the same file with `status` set to `blocked`, preserve every field that is known, and provide a specific evidence backed blocker. Do not claim acceptance when Company OS has not reached it.
