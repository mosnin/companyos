# Update Company OS kernels

Read the current project's requested versions first. For a web-bound project run
the trusted installed kernel manager's sync --project <path>. For local-only
projects run update-kernels --project <path>. Never broaden approved ranges, run
package lifecycle scripts or invent receipts. Preserve active state on failure.
See docs/kernels.md.
