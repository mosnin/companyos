interface:
  display_name: "Project Manager"
  short_description: "Force scoped plans and executable work breakdown"
  default_prompt: "Use $project-manager with $manage-company-program and one compact mission charter for project scoping, phase planning, work breakdown, risk management, or execution workflow work. Do not load this skill on the master or on Luna workers."
policy:
  allow_implicit_invocation: false
