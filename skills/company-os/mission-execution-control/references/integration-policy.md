# Continuous Integration Policy

Integration is continuous during the mission.

A product lane returns exact artifact manifests. The integration manager verifies current bytes, combines compatible slices, runs cross-lane checks, and checkpoints accepted increments. Integration is not deferred to the final mission hour.

Collisions or overlapping write ownership reduce concurrency. Additional agents are added only when integration throughput can absorb them.
