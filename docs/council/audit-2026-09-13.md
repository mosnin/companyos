# Board and model audit — 2026-09-13

## Scope

Audit the Company OS orchestration framework's native conversation hierarchy and
Council OS deliberation. Company OS Web is business context storage/display. The
live decision is a fictional arithmetic/strategy fixture, not authenticated business
context or a test of business efficacy.

## Defects corrected

- Reversed bootstrap: the previous role told an executive to create a projectless
  board. The entry conversation now establishes the board in the instance's host
  project; the board appoints executive conversations, which create management
  conversations and Luna worker teams in that same project.
- Executive bypass: the old coordinator's primary-thread-to-manager instruction
  now applies to each board-appointed executive's portfolio.
- Consultation collision: each decision has its own session binding and decision
  ID, so concurrent executive consultations do not overwrite one shared session.
- Conversation topology: the exported organization checker rejects duplicate or
  pending IDs, foreign instance/project bindings, wrong parent roles, missing
  message/acknowledgement references, and incorrect manager model/reasoning intent.
  References must still be checked against actual host outputs.
- Council records could reuse one runtime reference for every seat. Council now
  requires distinct member references in each stage and rejects malformed/nonfinite
  inputs. A valid record still requires 17 recommendations, 17 complete peer
  rankings, a memo, and exact request/roster binding.
- Sol was hardcoded beyond prose: manager templates, compilers, validators and
  provider requests now select gpt-6-astra with medium reasoning. Luna workers
  remain gpt-5.6-luna. Historical evidence is preserved, not relabeled as Astra.

## Execution boundary

Each board-appointed executive is the master of an existing executive → manager →
worker subtree. Multiple executives and multiple managers are supported. Arbitrary
manager-under-manager recursion is not supported by the current native execution
contract. Board deliberation uses distinct temporary member agents; executive,
manager and worker roles require persistent host-native project conversations.

The host agent performs actual native tool calls. Python validators check exported
artifacts and do not themselves create chats or enable background runtime adapters.
Requested model settings are kept separate from independently observed model identity.

## Local checks

- Main Company OS suite: 789 tests, one skipped, all remaining tests passed.
- Board handoff and organization checks: 18 passed after adding v2 session rebinding checks.
- Council routing/installer checks: 3 passed.
- Runtime suite: 334 passed.
- Transactional distribution suite: 22 passed.
- Council backend and packet checks: 14 passed.
- Role validator and 77 first-class skill surfaces passed.
- Canonical standalone installation verified after retaining an exact backup of
  the older installed bundles and their observed-byte manifest.

Live deliberation and native hierarchy results are recorded separately in the
local audit artifacts; local test success alone is not live acceptance.

## Observed native host behavior

A Git-worktree board launch returned a pending client ID; a canonical thread ID
could not be reconciled from the available task listing. A separate nonGit project
conversation did start, returned a canonical thread ID, and replied to the probe.
A follow-up message was delivered to that same conversation and its board turn began.
The task listing omitted this known conversation even though direct readback worked.
The role now explicitly reconciles stored canonical IDs before inferring deletion.

The board saved its executive-creation intent and then the host reported
`waitingOnApproval`. No executive creation result, manager, worker, or upward report
was observed. The exact pending approval action was not exposed by readback. This
is a blocked live hierarchy test, not proof of autonomous organization startup.
No approval setting was changed to make the test pass.

The initial native request explicitly configured Astra medium. The readback did not
independently expose provider model identity or prove sidebar project grouping. No
authenticated Company OS Web context was used in these fixture tests.

## Remaining gaps

- Complete the native board → executive → manager → worker run after the host
  approval gate is resolved; inspect actual child IDs, project membership, replies,
  restart/reconciliation, and observed model identity where available.
- Reconcile worktree launch completion without relying on a stale task listing.
- Arbitrary manager-under-manager recursion needs a versioned execution contract.
- Optional OpenRouter consultation has no persisted failed-seat retry checkpoint;
  restarting its CLI repeats successful calls. Native member retries can reuse
  returned agents when the host supports it.
- Cross-host behavior and business decision quality require their own evidence.

## Perspective fidelity

Each seat currently uses a short editorial business lens. The test can establish
separate reasoning calls, complete review and synthesis, and treatment of the
supplied evidence. It cannot establish authentic historical-person reasoning or
that 17 same-model perspectives outperform a strong single decision-maker. Those
claims would require broader comparative evaluation and better-grounded profiles.

## Completed direct Council trial

A real native-subagent run completed 17 distinct member responses, 17 complete
A–Q peer rankings, and neutral synthesis. Every original member was reused for its
review. The current Council validator passed; the root audit independently reran
it and inspected the memo and checks. The frozen response set remained unchanged.

The fixture correctly produced 20% activation and 50% retention among activated
users. The chair retained acquisition-fit, product-value and evidence-first
alternatives, small-sample uncertainty and delayed retention measurement. Peer
review caught several first drafts that blurred proposed experiments with current
authorization. All first-stage agents favored onboarding, and deferral remained
underexamined until review; this is a demonstrated limitation of the trial.

This passes the direct Council workflow on one bounded fictional case. It does not
pass the separate app hierarchy, executive adoption, host model-observation or real
business-context tests. Detailed records and native readback are retained locally
at `/Users/preston/company-os-audits/2026-09-13/`.
