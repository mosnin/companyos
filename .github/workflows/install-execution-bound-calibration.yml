name: Install Execution Bound Calibration

on:
  push:
    paths:
      - ".github/workflows/install-execution-bound-calibration.yml"

permissions:
  contents: write

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/company-os-outcome-control-plane
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install execution bound calibration
        run: |
          set -euo pipefail
          tar -xzf .company-os-tmp/execution-bound-calibration.tar.gz
          python3 .company-os-tmp/patch_outcome_control_calibration.py
          rm .company-os-tmp/execution-bound-calibration.tar.gz
          rm .company-os-tmp/patch_outcome_control_calibration.py
          rmdir .company-os-tmp
          rm .github/workflows/install-execution-bound-calibration.yml
      - name: Verify execution bound calibration
        run: |
          python3 -m unittest tests.test_evaluator_calibration -v
          python3 -m unittest tests.test_outcome_control -v
          python3 -m py_compile skills/company-os/calibrate-outcome-evaluator/scripts/calibrate_evaluator.py
      - name: Regenerate distribution manifest
        run: |
          python3 scripts/distribution.py write-manifest
          python3 scripts/distribution.py verify-manifest
      - name: Verify repository tests
        run: python3 -m unittest discover -s tests -v
      - name: Verify controller integration
        run: python3 skills/company-os/elastic-company-os/scripts/test_company_os_controller.py
      - name: Commit execution bound calibration
        run: |
          git config user.name "company-os-calibration-runtime-bot"
          git config user.email "company-os-calibration-runtime-bot@users.noreply.github.com"
          git add -A
          git commit -m "feat(company-os): derive calibration from evaluator executions"
          git push origin HEAD:codex/company-os-outcome-control-plane
